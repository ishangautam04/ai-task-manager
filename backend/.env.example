# Backend Environment Variables Template
# Copy this file to .env and fill in your actual values

# MongoDB Atlas Connection String
# Get free cluster at: https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskmanager?retryWrites=true&w=majority

# JWT Secret Key (IMPORTANT: Generate a strong random string!)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_recommended_64

# Server Configuration
PORT=5000
NODE_ENV=development

# AI Service API Keys
# Google Gemini AI (Free tier: 15 requests/minute)
# Get key at: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# Hugging Face (Optional - for additional AI features)
# Get free token at: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_token_here_optional

# Notes:
# - NEVER commit the actual .env file to Git
# - Each developer should create their own .env file
# - Use strong, unique keys for production
# - Keep API keys private and rotate them regularly
